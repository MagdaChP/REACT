import React from 'react';
import ReactDOM from 'react-dom';
document.addEventListener('DOMContentLoaded', function(){
    ReactDOM.render(
        <div> JSX a JS </div>,
        document.getElementById('app')
    );
});

// Umieść w pliku `js/zadanie00.jsx` odpowiedni kod, który ma wyrenderować na stronie w elemencie o id `app` element `div` 
// z napisem `JSX a JS`.